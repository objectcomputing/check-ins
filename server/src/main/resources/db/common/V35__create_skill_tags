drop table if exists skill_tags;
CREATE TABLE skill_tags(
   id varchar PRIMARY KEY,
   name varchar,
   description varchar
);

drop table if exists skills_skill_tags;
  CREATE TABLE member_skill_skill_tags(
     id varchar PRIMARY KEY,
     tag_id varchar REFERENCES skill_tags(id),
     skill_id varchar REFERENCES skills(id)
  );
