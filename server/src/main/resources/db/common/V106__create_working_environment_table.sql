CREATE TABLE working_environment(
        id varchar PRIMARY KEY,
        workLocation varchar,
        keyType varchar,
        osType varchar,
        accessories varchar,
        otherAccessories varchar
);