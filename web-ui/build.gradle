plugins {
    id "base"
    id "com.github.node-gradle.node" version "2.2.3"
}

node {
    version = '14.10.1' // https://nodejs.org/en/
    yarnVersion = '1.22.5' // https://yarnpkg.com/en/
    download = true
}

task start(type: YarnTask, dependsOn: 'yarn') {
    group = 'application'
    description = 'Run the client app'
    args = ['run', 'start']
}

task build(overwrite: true, type: YarnTask, dependsOn: 'yarn') {
    outputs.dir 'build'
    group = 'build'
    description = 'Build the client bundle'
    args = ['run', 'build']
}

task test(type: YarnTask, dependsOn: 'yarn') {
    outputs.dir 'build'
    group = 'verification'
    description = 'Run the client tests'
    args = ['run', 'test']
}

task coverage(type: YarnTask, dependsOn: 'yarn') {
    outputs.dir 'build'
    group = 'verification'
    description = 'Run the client tests with coverage'
    args = ['run', 'coverage']
}

task eject(type: YarnTask, dependsOn: 'yarn') {
    group = 'other'
    description = 'Eject from the create-react-app scripts'
    args = ['run', 'eject']
}

artifacts {
    'default' file: buildDir, type: 'directory', builtBy: yarn_run_build
}

yarn_run_build.outputs.dir 'build'
