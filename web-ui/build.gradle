plugins {
    id "base"
    id "com.github.node-gradle.node" version "2.2.3"
}

node {
    version = '14.10.1' // https://nodejs.org/en/
    yarnVersion = '1.22.5' // https://yarnpkg.com/en/
    download = true
}

yarn_run_coverage.outputs.dir 'coverage'
yarn_run_build.outputs.dir 'build'
