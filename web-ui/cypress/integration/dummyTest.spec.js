
// This test is purely written to ensure that Cypress is configured properly in github workflow.
describe('Dummy Test', () => {
  it('Does not do much!', () => {
    expect(true).to.equal(true)
  })
})