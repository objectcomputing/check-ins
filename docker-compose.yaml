version: '3'
services:
  postgresql:
    image: postgres:17.2
    environment:
      POSTGRES_DB: checkinsdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    container_name: checkinsdb
